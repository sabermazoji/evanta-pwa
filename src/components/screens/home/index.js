import React from 'react';
import Navbar from '../../elemenets/Navbar';
import Layout from '../../layouts';

const Home = () => {
    return (
        <Layout>
        <Navbar />
        </Layout>
    );
};

export default Home;
