import React from 'react';
import Navbar from '../../elemenets/Navbar';
import Layout from '../../layouts';

const Home = () => {
    return (
        <Layout>
        test
        </Layout>
    );
};

export default Home;
